// This page is intentionally left blank after reverting Firebase integration.
// If direct navigation occurs, it might result in a 404 or an empty page
// depending on routing setup post-reversion.
export default function LoginPage() {
  return null;
}
